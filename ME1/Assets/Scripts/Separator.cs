using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Separator : MonoBehaviour
{
    //Manager manager;
    //float minDist = 2;
    //Rigidbody rb;
    private void Start()
    {
        //rb = GetComponent<Rigidbody>();
        
    }

    private void FixedUpdate()
    {
        //foreach (Attractor attractor in manager.Attractors)
        //{
        //    float dist = Vector3.Distance(manager.Attractors[2].position, transform.position);
        //    if (dist < minDist)
        //    {
                

        //    }

        //}
            
    }

}
